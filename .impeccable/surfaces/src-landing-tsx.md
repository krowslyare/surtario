---
version: 1
slug: "src-landing-tsx"
primary_target: "src/Landing.tsx"
related_targets: ["src/components/LandingFlow.tsx", "src/styles/landing.css"]
---

# Surtario product entrance

Purpose: present Surtario as a restaurant ingredient-sourcing product, ready for a hackathon presentation. The user's latest direction supersedes prototype-centered marketing copy: explain the value and controls, without API setup or demo disclaimers in the CTA/FAQ. Keep fictional-price labels attached to the specific illustrative examples. Primary actions open the existing workspace. No invented savings, testimonials, subscription pricing or automatic purchasing claims.

## Direction contract

THESIS: Ingredient sourcing made tangible, from finding options to an optional purchasing decision. Readable product illustrations show the evidence at each step; one sample answer changes the visible order comparison.

OWN-WORLD: Retain the approved porcelain, aubergine, radish and lime palette, Bricolage display type, Manrope body and existing hero ingredient photograph. The closing CTA uses a continuous, atmospheric ingredient-sourcing photograph within this identity. Follow the visual authority referenced by `DESIGN.md`; this surface-specific composition introduces no new global tokens.

STORY: Understand the product and reveal an explicitly fictional supplier answer in the hero. Explore four stages: find options, keep evidence, clear up unknowns and decide when ready. Follow with the user's control over confirmation and browser-bound saved work. Close with “Try Surtario with one ingredient.”, a product-value paragraph, “Start sourcing” and “No recipes or purchase history needed.”, followed by compact FAQs.

FIRST VIEWPORT: Quiet brand/navigation row; generous left-aligned headline and short explanation at left; a large ingredient photograph at right, joined by one readable purchasing slip with the interactive decision. Primary action directly beneath the promise and an anchor to the walkthrough. The factual footer closes the full page.

FORM: Retain the split hero, walkthrough and integrated control section described below. The closing section is capped at 1200 px with one continuous photographic panel behind the existing invitation and action. A left-weighted aubergine overlay supports white text and a porcelain action; the scene remains visible across the panel, with no separate image column, cutout, figure or caption. The copy is capped at 520 px and the panel uses 40 px vertical / 56 px horizontal padding. FAQ follows below, with its title aligned to the invitation's left inset and accordion in the right column. Below 1000 px panel padding reduces to 40 px. Below 760 px use 32 px vertical / 24 px horizontal padding, a stronger uniform aubergine overlay and a 65% horizontal image position; the FAQ stacks below. Preserve global tokens.

LAYOUT REFINEMENT: The walkthrough selector is vertically centered against the adjacent panel on two-column layouts. The redundant “Illustrative walkthrough” caption and its location/unit banner are removed; sample labels and units remain within the examples. Mobile retains the stacked layout. Verified all four selections at 1920, 1440, 800, 390 and 320 px, with exact center alignment on two-column layouts and no horizontal overflow.

INTERACTION: Selecting a walkthrough step changes its associated semantic tab panel. Arrow keys, Home and End move selection and focus. Each numbered circle aligns with the first title line through a fixed grid column and matching circle height/title line-height (32 px desktop, 28 px mobile); a neutral vertical connector joins the sequence. Selection uses the existing pale tonal surface with a filled aubergine circle. Inactive hover uses the distinct neutral hover surface and retains an outlined circle; hovering another step must not imply selection. All panels occupy the same grid cell, reserving the tallest panel's height at the current width. Inactive panels remain in layout but are visually hidden, aria-hidden and inert, with no pointer interaction or tab stop. After the first selection interaction, the active panel enters with the existing selection duration (180 ms), opacity from 0.35 to 1 and a 6 px upward settling movement. The initial panel does not animate on load. Reduced motion disables panel animation and selector transitions. The existing sample-reply reveal remains reversible. No autoplay or animated amounts. Walkthrough illustrations use semantic HTML and Lucide symbols, not screenshot assets or a decorative icon grid.

ASSET: `public/brand/sourcing-cta.webp` is a generated 2172 × 724 editorial photograph of an ingredient receiving and planning table, with produce, grains, linen and a blank order sheet. It supplies atmosphere, not a supplier product photograph or sourcing evidence. It fills the panel with cover sizing behind the HTML copy and has empty alt text as decorative imagery. The image loads lazily with declared dimensions and asynchronous decoding. The exact generation prompt and provenance are retained in `public/brand/SOURCING_CTA_PROMPT.txt` and `public/brand/sourcing-cta.webp.json`. The previous rice cutout was removed. The existing hero photograph is unchanged.

CONTENT: The illustrative Rice / Portland example keeps USD and lb explicit, includes a distributor without a price, preserves pending terms and demonstrates whole packs, delivery, excess and order total. Research requires no quantity or recipe. A reviewed draft is not a sent message, and selecting an offer does not place an order. Illustrative suppliers and prices stay labeled in their examples. FAQ explains comparable pack/order costs, confirmation before sending, no automatic orders, and same-browser recovery. Provider configuration and test-recipient restrictions belong to the workspace, where the relevant actions occur.

FINISH: The earlier prototype-card review did not anticipate the user's rejection of its oversized lime ingredient column and disconnected FAQ. The subsequent pale pink card with a rice cutout was also rejected because the cutout and empty gap did not provide the requested artistic background. That composition has been replaced by the continuous photographic CTA described here. The replacement passed TypeScript, the production build and the two focused existing landing/keyboard browser journeys. Captures are recorded at `.local/pr37-cta-background/prototype-{1920,1440,390,320}.png`; the layout detector returned no findings (`[]`). Fresh independent visual review found no material CTA defects across all four captures; the replacement is ready for user review. This records local verification, not user design acceptance or deployment.

COPY REFINEMENT: CTA and FAQ now present product value per the latest user request. Removed prototype/hackathon language and the fictional-supplier FAQ; retained accurate confirmation, pending-cost and browser-recovery boundaries. TypeScript and two focused existing browser journeys passed.

SCROLL CONTINUITY: “See how Surtario works” uses native smooth scrolling with fragment history and focus transfer that does not jump first. Reduced motion selects instant scrolling. The shared helper also covers workspace search, summary, sourcing follow-ups, offer review and page-start actions. Native skip links and dialog/select focus handling retain their accessibility behavior. Rendered-position tests verify smooth versus reduced-motion scrolling; pointer verification recorded 30 positions on the landing transition.

HIERARCHY REFINEMENT: The photographic CTA remains the main visual emphasis in the close; its paragraph is one short sentence and its button is porcelain with aubergine text. The timeline connector is drawn per row, with its first/last ends anchored to circle centers, so wrapped descriptions cannot extend the line below step 4. Evidence for these refinements: `.local/pr37-cta-hierarchy/` and `.local/pr37-timeline-end/`.

CONTROL PRESENTATION: Preserve the original h2 and all three original headings and full paragraphs verbatim. The section sits directly on the page ground, capped at 1200 px, without a panel background, outer border, radius or horizontal padding. The heading wraps naturally at 28–34 px alongside the existing decorative Surtario symbol (48 px desktop, 32 px mobile). A single divider separates this compact header from three equal text columns with aligned paragraph starts through CSS subgrid. Below 760 px the columns stack. The adjacent photographic CTA is unchanged. No new information or review specimen is introduced. TypeScript and five viewport geometry checks passed; desktop/mobile screenshots were inspected. Current captures: `.local/pr37-control-section/section-{1920,1440,800,390,320}.png`. This records local verification, not user acceptance or deployment.
